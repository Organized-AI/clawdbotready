#!/usr/bin/env bun
// @generated by @gokhun/google-ads-mcp@0.1.0 on 2026-02-18T18:19:18.642Z. DO NOT EDIT.
import { Command } from 'commander';
import { createRuntime, createServerProxy } from 'mcporter';
import { createCallResult } from 'mcporter';

const embeddedServer = {
  "name": "google-ads-cli",
  "command": {
    "kind": "stdio",
    "command": "node",
    "args": [
      "dist/index.js"
    ]
  },
  "source": {
    "kind": "local",
    "path": "<adhoc>"
  },
  "description": "google-ads-mcp"
} as const;
const embeddedSchemas = {
  "list_campaigns": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "number",
        "description": "Maximum number of campaigns to return"
      },
      "includeRemoved": {
        "type": "boolean",
        "description": "Include removed campaigns"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_14_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH",
          "THIS_QUARTER",
          "LAST_QUARTER",
          "THIS_YEAR",
          "LAST_YEAR",
          "ALL_TIME",
          "CUSTOM"
        ],
        "description": "Date range for metrics (default: ALL_TIME)"
      },
      "customDateRange": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "description": "Start date in YYYY-MM-DD format"
          },
          "endDate": {
            "type": "string",
            "description": "End date in YYYY-MM-DD format"
          }
        },
        "required": [
          "startDate",
          "endDate"
        ],
        "description": "Custom date range (only used when dateRange is CUSTOM)"
      }
    }
  },
  "get_campaign": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Campaign ID"
      }
    },
    "required": [
      "campaignId"
    ]
  },
  "create_campaign": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Campaign name"
      },
      "budget": {
        "type": "number",
        "description": "Daily budget in account currency"
      },
      "advertisingChannelType": {
        "type": "string",
        "enum": [
          "SEARCH",
          "DISPLAY",
          "SHOPPING",
          "VIDEO",
          "MULTI_CHANNEL"
        ],
        "description": "Campaign type"
      },
      "status": {
        "type": "string",
        "enum": [
          "ENABLED",
          "PAUSED"
        ],
        "description": "Campaign status"
      }
    },
    "required": [
      "name",
      "budget",
      "advertisingChannelType"
    ]
  },
  "update_campaign": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Campaign ID"
      },
      "name": {
        "type": "string",
        "description": "New campaign name"
      },
      "status": {
        "type": "string",
        "enum": [
          "ENABLED",
          "PAUSED",
          "REMOVED"
        ],
        "description": "New campaign status"
      },
      "budget": {
        "type": "number",
        "description": "New daily budget"
      }
    },
    "required": [
      "campaignId"
    ]
  },
  "list_ad_groups": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Filter by campaign ID (optional)"
      },
      "limit": {
        "type": "number",
        "description": "Maximum number of ad groups to return"
      },
      "includeRemoved": {
        "type": "boolean",
        "description": "Include removed ad groups"
      }
    }
  },
  "create_ad_group": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Campaign ID where the ad group will be created"
      },
      "name": {
        "type": "string",
        "description": "Name of the ad group"
      },
      "status": {
        "type": "string",
        "enum": [
          "ENABLED",
          "PAUSED"
        ],
        "description": "Initial status of the ad group"
      },
      "cpcBidMicros": {
        "type": "number",
        "description": "Cost per click bid in micros (1 million = 1 currency unit)"
      },
      "cpmBidMicros": {
        "type": "number",
        "description": "Cost per thousand impressions bid in micros"
      },
      "targetCpaMicros": {
        "type": "number",
        "description": "Target cost per acquisition in micros"
      },
      "targetRoas": {
        "type": "number",
        "description": "Target return on ad spend"
      }
    },
    "required": [
      "campaignId",
      "name"
    ]
  },
  "update_ad_group": {
    "type": "object",
    "properties": {
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID to update"
      },
      "name": {
        "type": "string",
        "description": "New name for the ad group"
      },
      "status": {
        "type": "string",
        "enum": [
          "ENABLED",
          "PAUSED",
          "REMOVED"
        ],
        "description": "New status for the ad group"
      },
      "cpcBidMicros": {
        "type": "number",
        "description": "New CPC bid in micros"
      },
      "cpmBidMicros": {
        "type": "number",
        "description": "New CPM bid in micros"
      },
      "targetCpaMicros": {
        "type": "number",
        "description": "New target CPA in micros"
      },
      "targetRoas": {
        "type": "number",
        "description": "New target ROAS"
      }
    },
    "required": [
      "adGroupId"
    ]
  },
  "get_ad_group": {
    "type": "object",
    "properties": {
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID"
      }
    },
    "required": [
      "adGroupId"
    ]
  },
  "list_ads": {
    "type": "object",
    "properties": {
      "adGroupId": {
        "type": "string",
        "description": "Filter by ad group ID (optional)"
      },
      "campaignId": {
        "type": "string",
        "description": "Filter by campaign ID (optional)"
      },
      "limit": {
        "type": "number",
        "description": "Maximum number of ads to return"
      },
      "includeRemoved": {
        "type": "boolean",
        "description": "Include removed ads"
      }
    }
  },
  "create_responsive_search_ad": {
    "type": "object",
    "properties": {
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID where the ad will be created"
      },
      "headlines": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "text": {
              "type": "string",
              "description": "Headline text (max 30 characters)"
            },
            "pinned_field": {
              "type": "number",
              "description": "Position to pin this headline (1-3, optional)"
            }
          },
          "required": [
            "text"
          ]
        },
        "description": "Headlines for the ad (3-15 headlines)"
      },
      "descriptions": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "text": {
              "type": "string",
              "description": "Description text (max 90 characters)"
            },
            "pinned_field": {
              "type": "number",
              "description": "Position to pin this description (1-2, optional)"
            }
          },
          "required": [
            "text"
          ]
        },
        "description": "Descriptions for the ad (2-4 descriptions)"
      },
      "path1": {
        "type": "string",
        "description": "First path component (max 15 characters)"
      },
      "path2": {
        "type": "string",
        "description": "Second path component (max 15 characters)"
      },
      "finalUrls": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Landing page URLs"
      },
      "finalMobileUrls": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Mobile landing page URLs (optional)"
      },
      "trackingUrlTemplate": {
        "type": "string",
        "description": "Tracking URL template (optional)"
      }
    },
    "required": [
      "adGroupId",
      "headlines",
      "descriptions",
      "finalUrls"
    ]
  },
  "update_ad": {
    "type": "object",
    "properties": {
      "adId": {
        "type": "string",
        "description": "Ad ID to update"
      },
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID containing the ad"
      },
      "status": {
        "type": "string",
        "enum": [
          "ENABLED",
          "PAUSED",
          "REMOVED"
        ],
        "description": "New status for the ad"
      }
    },
    "required": [
      "adId",
      "adGroupId"
    ]
  },
  "get_ad_performance": {
    "type": "object",
    "properties": {
      "adId": {
        "type": "string",
        "description": "Ad ID"
      },
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID containing the ad"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH",
          "ALL_TIME"
        ],
        "description": "Date range for metrics"
      }
    },
    "required": [
      "adId",
      "adGroupId"
    ]
  },
  "list_keywords": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Filter by campaign ID"
      },
      "adGroupId": {
        "type": "string",
        "description": "Filter by ad group ID"
      },
      "limit": {
        "type": "number",
        "description": "Maximum number of keywords to return"
      },
      "includeNegative": {
        "type": "boolean",
        "description": "Include negative keywords"
      }
    }
  },
  "add_keywords": {
    "type": "object",
    "properties": {
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID"
      },
      "keywords": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "text": {
              "type": "string",
              "description": "Keyword text"
            },
            "matchType": {
              "type": "string",
              "enum": [
                "EXACT",
                "PHRASE",
                "BROAD"
              ],
              "description": "Keyword match type"
            },
            "cpcBidMicros": {
              "type": "number",
              "description": "CPC bid in micros (optional)"
            }
          },
          "required": [
            "text",
            "matchType"
          ]
        }
      }
    },
    "required": [
      "adGroupId",
      "keywords"
    ]
  },
  "add_negative_keywords": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Campaign ID (for campaign-level negatives)"
      },
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID (for ad group-level negatives)"
      },
      "keywords": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "text": {
              "type": "string",
              "description": "Keyword text"
            },
            "matchType": {
              "type": "string",
              "enum": [
                "EXACT",
                "PHRASE",
                "BROAD"
              ],
              "description": "Keyword match type"
            }
          },
          "required": [
            "text",
            "matchType"
          ]
        }
      }
    },
    "required": [
      "keywords"
    ]
  },
  "update_keyword": {
    "type": "object",
    "properties": {
      "keywordId": {
        "type": "string",
        "description": "Keyword ID"
      },
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID"
      },
      "status": {
        "type": "string",
        "enum": [
          "ENABLED",
          "PAUSED",
          "REMOVED"
        ],
        "description": "New keyword status"
      },
      "cpcBidMicros": {
        "type": "number",
        "description": "New CPC bid in micros"
      }
    },
    "required": [
      "keywordId",
      "adGroupId"
    ]
  },
  "get_keyword_performance": {
    "type": "object",
    "properties": {
      "keywordId": {
        "type": "string",
        "description": "Keyword ID"
      },
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH"
        ],
        "description": "Date range for metrics"
      }
    },
    "required": [
      "keywordId",
      "adGroupId"
    ]
  },
  "get_account_performance": {
    "type": "object",
    "properties": {
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_14_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH",
          "THIS_QUARTER",
          "LAST_QUARTER"
        ],
        "description": "Date range for metrics"
      },
      "segmentByDate": {
        "type": "boolean",
        "description": "Segment results by date"
      }
    }
  },
  "get_campaign_performance": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Campaign ID"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_14_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH"
        ],
        "description": "Date range for metrics"
      },
      "segmentByDate": {
        "type": "boolean",
        "description": "Segment results by date"
      }
    },
    "required": [
      "campaignId"
    ]
  },
  "get_ad_group_performance": {
    "type": "object",
    "properties": {
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID (optional)"
      },
      "campaignId": {
        "type": "string",
        "description": "Filter by campaign ID (optional)"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH"
        ],
        "description": "Date range for metrics"
      },
      "limit": {
        "type": "number",
        "description": "Maximum number of ad groups to return"
      }
    }
  },
  "get_search_terms_report": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Filter by campaign ID (optional)"
      },
      "adGroupId": {
        "type": "string",
        "description": "Filter by ad group ID (optional)"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH",
          "CUSTOM"
        ],
        "description": "Date range for report"
      },
      "customDateRange": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "description": "Start date in YYYY-MM-DD format"
          },
          "endDate": {
            "type": "string",
            "description": "End date in YYYY-MM-DD format"
          }
        },
        "required": [
          "startDate",
          "endDate"
        ],
        "description": "Custom date range (required when dateRange is CUSTOM)"
      },
      "limit": {
        "type": "number",
        "description": "Maximum number of search terms to return"
      },
      "minImpressions": {
        "type": "number",
        "description": "Minimum impressions threshold"
      }
    }
  },
  "list_accessible_customers": {
    "type": "object",
    "properties": {}
  },
  "get_account_hierarchy": {
    "type": "object",
    "properties": {
      "customerId": {
        "type": "string",
        "description": "Customer ID to get hierarchy for (optional)"
      },
      "loginCustomerId": {
        "type": "string",
        "description": "Login customer ID for manager accounts (optional)"
      }
    }
  },
  "get_account_info": {
    "type": "object",
    "properties": {
      "customerId": {
        "type": "string",
        "description": "Customer ID to get info for"
      }
    },
    "required": [
      "customerId"
    ]
  },
  "list_manager_accounts": {
    "type": "object",
    "properties": {
      "customerId": {
        "type": "string",
        "description": "Customer ID to check manager relationships (optional)"
      }
    }
  },
  "get_top_bottom_keywords": {
    "type": "object",
    "properties": {
      "metric": {
        "type": "string",
        "enum": [
          "COST",
          "CLICKS",
          "CONVERSIONS",
          "CTR",
          "CONVERSION_RATE",
          "CPC",
          "QUALITY_SCORE"
        ],
        "description": "Metric to rank keywords by"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH"
        ],
        "description": "Date range for analysis"
      },
      "topCount": {
        "type": "number",
        "description": "Number of top performers to return"
      },
      "bottomCount": {
        "type": "number",
        "description": "Number of bottom performers to return"
      },
      "campaignId": {
        "type": "string",
        "description": "Filter by specific campaign ID (optional)"
      },
      "adGroupId": {
        "type": "string",
        "description": "Filter by specific ad group ID (optional)"
      },
      "includeNegative": {
        "type": "boolean",
        "description": "Include negative keywords in analysis"
      }
    },
    "required": [
      "metric"
    ]
  },
  "get_keyword_opportunities": {
    "type": "object",
    "properties": {
      "dateRange": {
        "type": "string",
        "enum": [
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "LAST_90_DAYS"
        ],
        "description": "Date range for analysis"
      },
      "minImpressions": {
        "type": "number",
        "description": "Minimum impressions threshold"
      },
      "maxCostPerConversion": {
        "type": "number",
        "description": "Maximum acceptable cost per conversion"
      },
      "minConversionRate": {
        "type": "number",
        "description": "Minimum conversion rate percentage"
      }
    }
  },
  "get_campaign_comparison": {
    "type": "object",
    "properties": {
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH"
        ],
        "description": "Date range for comparison"
      },
      "metric": {
        "type": "string",
        "enum": [
          "COST",
          "CLICKS",
          "CONVERSIONS",
          "ROAS",
          "CPA"
        ],
        "description": "Metric to sort campaigns by"
      },
      "includeRemoved": {
        "type": "boolean",
        "description": "Include removed campaigns in comparison"
      }
    }
  },
  "list_conversion_actions": {
    "type": "object",
    "properties": {
      "includeRemoved": {
        "type": "boolean",
        "description": "Include removed conversion actions"
      }
    }
  },
  "create_conversion_action": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the conversion action"
      },
      "category": {
        "type": "string",
        "enum": [
          "PURCHASE",
          "SIGNUP",
          "LEAD",
          "PAGE_VIEW",
          "DOWNLOAD",
          "OTHER"
        ],
        "description": "Category of the conversion"
      },
      "type": {
        "type": "string",
        "enum": [
          "WEBPAGE",
          "UPLOAD_CLICKS",
          "AD_CALL",
          "APP_DOWNLOAD"
        ],
        "description": "Type of conversion tracking"
      },
      "status": {
        "type": "string",
        "enum": [
          "ENABLED",
          "REMOVED",
          "HIDDEN"
        ],
        "description": "Status of the conversion action"
      },
      "valueSettings": {
        "type": "object",
        "properties": {
          "defaultValue": {
            "type": "number",
            "description": "Default conversion value"
          },
          "defaultCurrencyCode": {
            "type": "string",
            "description": "Currency code (e.g., USD, EUR)"
          },
          "alwaysUseDefaultValue": {
            "type": "boolean",
            "description": "Always use the default value"
          }
        }
      },
      "countingType": {
        "type": "string",
        "enum": [
          "ONE_PER_CLICK",
          "MANY_PER_CLICK"
        ],
        "description": "How to count conversions"
      },
      "attributionModel": {
        "type": "string",
        "enum": [
          "EXTERNAL",
          "GOOGLE_ADS_LAST_CLICK",
          "GOOGLE_SEARCH_ATTRIBUTION_FIRST_CLICK",
          "GOOGLE_SEARCH_ATTRIBUTION_LINEAR",
          "GOOGLE_SEARCH_ATTRIBUTION_TIME_DECAY",
          "GOOGLE_SEARCH_ATTRIBUTION_POSITION_BASED",
          "GOOGLE_SEARCH_ATTRIBUTION_DATA_DRIVEN"
        ],
        "description": "Attribution model to use"
      },
      "clickThroughLookbackWindowDays": {
        "type": "number",
        "description": "Click-through conversion window in days (1-90)"
      },
      "viewThroughLookbackWindowDays": {
        "type": "number",
        "description": "View-through conversion window in days (1-30)"
      }
    },
    "required": [
      "name",
      "category"
    ]
  },
  "update_conversion_action": {
    "type": "object",
    "properties": {
      "conversionActionId": {
        "type": "string",
        "description": "ID of the conversion action to update"
      },
      "name": {
        "type": "string",
        "description": "New name for the conversion action"
      },
      "status": {
        "type": "string",
        "enum": [
          "ENABLED",
          "REMOVED",
          "HIDDEN"
        ],
        "description": "New status"
      },
      "valueSettings": {
        "type": "object",
        "properties": {
          "defaultValue": {
            "type": "number",
            "description": "Default conversion value"
          },
          "defaultCurrencyCode": {
            "type": "string",
            "description": "Currency code"
          },
          "alwaysUseDefaultValue": {
            "type": "boolean",
            "description": "Always use the default value"
          }
        }
      },
      "countingType": {
        "type": "string",
        "enum": [
          "ONE_PER_CLICK",
          "MANY_PER_CLICK"
        ],
        "description": "How to count conversions"
      },
      "attributionModel": {
        "type": "string",
        "enum": [
          "EXTERNAL",
          "GOOGLE_ADS_LAST_CLICK",
          "GOOGLE_SEARCH_ATTRIBUTION_FIRST_CLICK",
          "GOOGLE_SEARCH_ATTRIBUTION_LINEAR",
          "GOOGLE_SEARCH_ATTRIBUTION_TIME_DECAY",
          "GOOGLE_SEARCH_ATTRIBUTION_POSITION_BASED",
          "GOOGLE_SEARCH_ATTRIBUTION_DATA_DRIVEN"
        ],
        "description": "Attribution model"
      },
      "clickThroughLookbackWindowDays": {
        "type": "number",
        "description": "Click-through window in days"
      },
      "viewThroughLookbackWindowDays": {
        "type": "number",
        "description": "View-through window in days"
      }
    },
    "required": [
      "conversionActionId"
    ]
  },
  "get_conversion_stats": {
    "type": "object",
    "properties": {
      "conversionActionId": {
        "type": "string",
        "description": "Filter by specific conversion action ID (optional)"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH",
          "ALL_TIME"
        ],
        "description": "Date range for statistics"
      },
      "segmentByConversionAction": {
        "type": "boolean",
        "description": "Show breakdown by conversion action"
      }
    }
  },
  "get_product_performance": {
    "type": "object",
    "properties": {
      "campaignId": {
        "type": "string",
        "description": "Filter by specific campaign ID (optional)"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH",
          "ALL_TIME"
        ],
        "description": "Date range for metrics"
      },
      "limit": {
        "type": "number",
        "description": "Maximum number of products to return"
      },
      "orderBy": {
        "type": "string",
        "enum": [
          "COST",
          "CLICKS",
          "CONVERSIONS",
          "REVENUE",
          "ROAS"
        ],
        "description": "Metric to order by"
      },
      "orderDirection": {
        "type": "string",
        "enum": [
          "ASC",
          "DESC"
        ],
        "description": "Order direction"
      }
    }
  },
  "get_product_partition_performance": {
    "type": "object",
    "properties": {
      "adGroupId": {
        "type": "string",
        "description": "Ad group ID containing the product partitions"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH",
          "ALL_TIME"
        ],
        "description": "Date range for metrics"
      }
    },
    "required": [
      "adGroupId"
    ]
  },
  "get_top_bottom_products": {
    "type": "object",
    "properties": {
      "metric": {
        "type": "string",
        "enum": [
          "COST",
          "CLICKS",
          "CONVERSIONS",
          "REVENUE",
          "ROAS",
          "CTR",
          "CONVERSION_RATE"
        ],
        "description": "Metric to rank products by"
      },
      "dateRange": {
        "type": "string",
        "enum": [
          "TODAY",
          "YESTERDAY",
          "LAST_7_DAYS",
          "LAST_30_DAYS",
          "THIS_MONTH",
          "LAST_MONTH"
        ],
        "description": "Date range for analysis"
      },
      "topCount": {
        "type": "number",
        "description": "Number of top performers to return"
      },
      "bottomCount": {
        "type": "number",
        "description": "Number of bottom performers to return"
      },
      "campaignId": {
        "type": "string",
        "description": "Filter by specific campaign ID (optional)"
      }
    },
    "required": [
      "metric"
    ]
  }
} as const;
const embeddedName = "google-ads-cli";
const embeddedDescription = "google-ads-mcp";
const generatorInfo = "Generated by @gokhun/google-ads-mcp@0.1.0 — https://github.com/steipete/mcporter";
const generatorTools = [
  {
    "name": "list-campaigns",
    "description": "List all Google Ads campaigns with their metrics for a specific date range",
    "usage": "list-campaigns [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER|THIS_YEAR|LAST_YEAR|ALL_TIME|CUSTOM>] [--custom-date-range <custom-date-range>] [--raw <json>]",
    "flags": "[--limit <limit:number>] [--include-removed <include-removed:true|false>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER|THIS_YEAR|LAST_YEAR|ALL_TIME|CUSTOM>] [--custom-date-range <custom-date-range>] [--raw <json>]"
  },
  {
    "name": "get-campaign",
    "description": "Get detailed information about a specific campaign",
    "usage": "get-campaign --campaign-id <campaign-id> [--raw <json>]",
    "flags": "--campaign-id <campaign-id> [--raw <json>]"
  },
  {
    "name": "create-campaign",
    "description": "Create a new Google Ads campaign",
    "usage": "create-campaign --name <name> --budget <budget:number> --advertising-channel-type <advertising-channel-type:SEARCH|DISPLAY|SHOPPING|VIDEO|MULTI_CHANNEL> [--status <status:ENABLED|PAUSED>] [--raw <json>]",
    "flags": "--name <name> --budget <budget:number> --advertising-channel-type <advertising-channel-type:SEARCH|DISPLAY|SHOPPING|VIDEO|MULTI_CHANNEL> [--status <status:ENABLED|PAUSED>] [--raw <json>]"
  },
  {
    "name": "update-campaign",
    "description": "Update an existing campaign",
    "usage": "update-campaign --campaign-id <campaign-id> [--name <name>] [--status <status:ENABLED|PAUSED|REMOVED>] [--budget <budget:number>] [--raw <json>]",
    "flags": "--campaign-id <campaign-id> [--name <name>] [--status <status:ENABLED|PAUSED|REMOVED>] [--budget <budget:number>] [--raw <json>]"
  },
  {
    "name": "list-ad-groups",
    "description": "List ad groups with their performance metrics",
    "usage": "list-ad-groups [--campaign-id <campaign-id>] [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--raw <json>]",
    "flags": "[--campaign-id <campaign-id>] [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--raw <json>]"
  },
  {
    "name": "create-ad-group",
    "description": "Create a new ad group in a campaign",
    "usage": "create-ad-group --campaign-id <campaign-id> --name <name> [--status <status:ENABLED|PAUSED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--cpm-bid-micros <cpm-bid-micros:number>] [--raw <json>]",
    "flags": "--campaign-id <campaign-id> --name <name> [--status <status:ENABLED|PAUSED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--cpm-bid-micros <cpm-bid-micros:number>] [--raw <json>]"
  },
  {
    "name": "update-ad-group",
    "description": "Update an existing ad group",
    "usage": "update-ad-group --ad-group-id <ad-group-id> [--name <name>] [--status <status:ENABLED|PAUSED|REMOVED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--cpm-bid-micros <cpm-bid-micros:number>] [--raw <json>]",
    "flags": "--ad-group-id <ad-group-id> [--name <name>] [--status <status:ENABLED|PAUSED|REMOVED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--cpm-bid-micros <cpm-bid-micros:number>] [--raw <json>]"
  },
  {
    "name": "get-ad-group",
    "description": "Get detailed information about a specific ad group",
    "usage": "get-ad-group --ad-group-id <ad-group-id> [--raw <json>]",
    "flags": "--ad-group-id <ad-group-id> [--raw <json>]"
  },
  {
    "name": "list-ads",
    "description": "List ads with their performance metrics",
    "usage": "list-ads [--ad-group-id <ad-group-id>] [--campaign-id <campaign-id>] [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--raw <json>]",
    "flags": "[--ad-group-id <ad-group-id>] [--campaign-id <campaign-id>] [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--raw <json>]"
  },
  {
    "name": "create-responsive-search-ad",
    "description": "Create a new responsive search ad",
    "usage": "create-responsive-search-ad --ad-group-id <ad-group-id> --headlines <headlines:value1,value2> --descriptions <descriptions:value1,value2> [--path1 <path1>] --final-urls <final-urls:value1,value2> [--raw <json>]",
    "flags": "--ad-group-id <ad-group-id> --headlines <headlines:value1,value2> --descriptions <descriptions:value1,value2> [--path1 <path1>] --final-urls <final-urls:value1,value2> [--raw <json>]"
  },
  {
    "name": "update-ad",
    "description": "Update an existing ad status",
    "usage": "update-ad --ad-id <ad-id> --ad-group-id <ad-group-id> [--status <status:ENABLED|PAUSED|REMOVED>] [--raw <json>]",
    "flags": "--ad-id <ad-id> --ad-group-id <ad-group-id> [--status <status:ENABLED|PAUSED|REMOVED>] [--raw <json>]"
  },
  {
    "name": "get-ad-performance",
    "description": "Get detailed performance metrics for a specific ad",
    "usage": "get-ad-performance --ad-id <ad-id> --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--raw <json>]",
    "flags": "--ad-id <ad-id> --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--raw <json>]"
  },
  {
    "name": "list-keywords",
    "description": "List keywords with their performance metrics",
    "usage": "list-keywords [--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] [--limit <limit:number>] [--include-negative <include-negative:true|false>] [--raw <json>]",
    "flags": "[--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] [--limit <limit:number>] [--include-negative <include-negative:true|false>] [--raw <json>]"
  },
  {
    "name": "add-keywords",
    "description": "Add new keywords to an ad group",
    "usage": "add-keywords --ad-group-id <ad-group-id> --keywords <keywords:value1,value2> [--raw <json>]",
    "flags": "--ad-group-id <ad-group-id> --keywords <keywords:value1,value2> [--raw <json>]"
  },
  {
    "name": "add-negative-keywords",
    "description": "Add negative keywords to a campaign or ad group",
    "usage": "add-negative-keywords [--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] --keywords <keywords:value1,value2> [--raw <json>]",
    "flags": "[--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] --keywords <keywords:value1,value2> [--raw <json>]"
  },
  {
    "name": "update-keyword",
    "description": "Update keyword status or bid",
    "usage": "update-keyword --keyword-id <keyword-id> --ad-group-id <ad-group-id> [--status <status:ENABLED|PAUSED|REMOVED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--raw <json>]",
    "flags": "--keyword-id <keyword-id> --ad-group-id <ad-group-id> [--status <status:ENABLED|PAUSED|REMOVED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--raw <json>]"
  },
  {
    "name": "get-keyword-performance",
    "description": "Get detailed performance metrics for a keyword",
    "usage": "get-keyword-performance --keyword-id <keyword-id> --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--raw <json>]",
    "flags": "--keyword-id <keyword-id> --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--raw <json>]"
  },
  {
    "name": "get-account-performance",
    "description": "Get overall account performance metrics",
    "usage": "get-account-performance [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER>] [--segment-by-date <segment-by-date:true|false>] [--raw <json>]",
    "flags": "[--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER>] [--segment-by-date <segment-by-date:true|false>] [--raw <json>]"
  },
  {
    "name": "get-campaign-performance",
    "description": "Get detailed campaign performance metrics",
    "usage": "get-campaign-performance --campaign-id <campaign-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--segment-by-date <segment-by-date:true|false>] [--raw <json>]",
    "flags": "--campaign-id <campaign-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--segment-by-date <segment-by-date:true|false>] [--raw <json>]"
  },
  {
    "name": "get-ad-group-performance",
    "description": "Get ad group performance metrics",
    "usage": "get-ad-group-performance [--ad-group-id <ad-group-id>] [--campaign-id <campaign-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--limit <limit:number>] [--raw <json>]",
    "flags": "[--ad-group-id <ad-group-id>] [--campaign-id <campaign-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--limit <limit:number>] [--raw <json>]"
  },
  {
    "name": "get-search-terms-report",
    "description": "Get search terms report to find new keyword opportunities",
    "usage": "get-search-terms-report [--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|CUSTOM>] [--custom-date-range <custom-date-range>] [--limit <limit:number>] [--raw <json>]",
    "flags": "[--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|CUSTOM>] [--custom-date-range <custom-date-range>] [--limit <limit:number>] [--raw <json>]"
  },
  {
    "name": "list-accessible-customers",
    "description": "List all Google Ads accounts accessible by the authenticated user",
    "usage": "list-accessible-customers [--raw <json>]",
    "flags": "[--raw <json>]"
  },
  {
    "name": "get-account-hierarchy",
    "description": "Get the account hierarchy showing manager and client relationships",
    "usage": "get-account-hierarchy [--customer-id <customer-id>] [--login-customer-id <login-customer-id>] [--raw <json>]",
    "flags": "[--customer-id <customer-id>] [--login-customer-id <login-customer-id>] [--raw <json>]"
  },
  {
    "name": "get-account-info",
    "description": "Get detailed information about a specific Google Ads account",
    "usage": "get-account-info --customer-id <customer-id> [--raw <json>]",
    "flags": "--customer-id <customer-id> [--raw <json>]"
  },
  {
    "name": "list-manager-accounts",
    "description": "List manager account relationships",
    "usage": "list-manager-accounts [--customer-id <customer-id>] [--raw <json>]",
    "flags": "[--customer-id <customer-id>] [--raw <json>]"
  },
  {
    "name": "get-top-bottom-keywords",
    "description": "Get top and bottom performing keywords by various metrics",
    "usage": "get-top-bottom-keywords --metric <metric:COST|CLICKS|CONVERSIONS|CTR|CONVERSION_RATE|CPC|QUALITY_SCORE> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--top-count <top-count:number>] [--bottom-count <bottom-count:number>] [--campaign-id <campaign-id>] [--raw <json>]",
    "flags": "--metric <metric:COST|CLICKS|CONVERSIONS|CTR|CONVERSION_RATE|CPC|QUALITY_SCORE> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--top-count <top-count:number>] [--bottom-count <bottom-count:number>] [--campaign-id <campaign-id>] [--raw <json>]"
  },
  {
    "name": "get-keyword-opportunities",
    "description": "Find keyword opportunities based on search terms and underperforming keywords",
    "usage": "get-keyword-opportunities [--date-range <date-range:LAST_7_DAYS|LAST_30_DAYS|LAST_90_DAYS>] [--min-impressions <min-impressions:number>] [--max-cost-per-conversion <max-cost-per-conversion:number>] [--min-conversion-rate <min-conversion-rate:number>] [--raw <json>]",
    "flags": "[--date-range <date-range:LAST_7_DAYS|LAST_30_DAYS|LAST_90_DAYS>] [--min-impressions <min-impressions:number>] [--max-cost-per-conversion <max-cost-per-conversion:number>] [--min-conversion-rate <min-conversion-rate:number>] [--raw <json>]"
  },
  {
    "name": "get-campaign-comparison",
    "description": "Compare all campaigns performance by various metrics",
    "usage": "get-campaign-comparison [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--metric <metric:COST|CLICKS|CONVERSIONS|ROAS|CPA>] [--include-removed <include-removed:true|false>] [--raw <json>]",
    "flags": "[--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--metric <metric:COST|CLICKS|CONVERSIONS|ROAS|CPA>] [--include-removed <include-removed:true|false>] [--raw <json>]"
  },
  {
    "name": "list-conversion-actions",
    "description": "List all conversion actions configured in the account",
    "usage": "list-conversion-actions [--include-removed <include-removed:true|false>] [--raw <json>]",
    "flags": "[--include-removed <include-removed:true|false>] [--raw <json>]"
  },
  {
    "name": "create-conversion-action",
    "description": "Create a new conversion action",
    "usage": "create-conversion-action --name <name> --category <category:PURCHASE|SIGNUP|LEAD|PAGE_VIEW|DOWNLOAD|OTHER> [--type <type:WEBPAGE|UPLOAD_CLICKS|AD_CALL|APP_DOWNLOAD>] [--status <status:ENABLED|REMOVED|HIDDEN>] [--value-settings <value-settings>] [--raw <json>]",
    "flags": "--name <name> --category <category:PURCHASE|SIGNUP|LEAD|PAGE_VIEW|DOWNLOAD|OTHER> [--type <type:WEBPAGE|UPLOAD_CLICKS|AD_CALL|APP_DOWNLOAD>] [--status <status:ENABLED|REMOVED|HIDDEN>] [--value-settings <value-settings>] [--raw <json>]"
  },
  {
    "name": "update-conversion-action",
    "description": "Update an existing conversion action",
    "usage": "update-conversion-action --conversion-action-id <conversion-action-id> [--name <name>] [--status <status:ENABLED|REMOVED|HIDDEN>] [--value-settings <value-settings>] [--counting-type <counting-type:ONE_PER_CLICK|MANY_PER_CLICK>] [--raw <json>]",
    "flags": "--conversion-action-id <conversion-action-id> [--name <name>] [--status <status:ENABLED|REMOVED|HIDDEN>] [--value-settings <value-settings>] [--counting-type <counting-type:ONE_PER_CLICK|MANY_PER_CLICK>] [--raw <json>]"
  },
  {
    "name": "get-conversion-stats",
    "description": "Get conversion statistics and performance",
    "usage": "get-conversion-stats [--conversion-action-id <conversion-action-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--segment-by-conversion-action <segment-by-conversion-action:true|false>] [--raw <json>]",
    "flags": "[--conversion-action-id <conversion-action-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--segment-by-conversion-action <segment-by-conversion-action:true|false>] [--raw <json>]"
  },
  {
    "name": "get-product-performance",
    "description": "Get performance data for products in shopping campaigns",
    "usage": "get-product-performance [--campaign-id <campaign-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--limit <limit:number>] [--order-by <order-by:COST|CLICKS|CONVERSIONS|REVENUE|ROAS>] [--order-direction <order-direction:ASC|DESC>] [--raw <json>]",
    "flags": "[--campaign-id <campaign-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--limit <limit:number>] [--order-by <order-by:COST|CLICKS|CONVERSIONS|REVENUE|ROAS>] [--order-direction <order-direction:ASC|DESC>] [--raw <json>]"
  },
  {
    "name": "get-product-partition-performance",
    "description": "Get performance data for product partitions in an ad group",
    "usage": "get-product-partition-performance --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--raw <json>]",
    "flags": "--ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--raw <json>]"
  },
  {
    "name": "get-top-bottom-products",
    "description": "Get top and bottom performing products by various metrics",
    "usage": "get-top-bottom-products --metric <metric:COST|CLICKS|CONVERSIONS|REVENUE|ROAS|CTR|CONVERSION_RATE> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--top-count <top-count:number>] [--bottom-count <bottom-count:number>] [--campaign-id <campaign-id>] [--raw <json>]",
    "flags": "--metric <metric:COST|CLICKS|CONVERSIONS|REVENUE|ROAS|CTR|CONVERSION_RATE> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--top-count <top-count:number>] [--bottom-count <bottom-count:number>] [--campaign-id <campaign-id>] [--raw <json>]"
  }
] as const;
const embeddedMetadata = {
  "schemaVersion": 1,
  "generatedAt": "2026-02-18T18:19:18.642Z",
  "generator": {
    "name": "@gokhun/google-ads-mcp",
    "version": "0.1.0"
  },
  "server": {
    "name": "google-ads-cli",
    "source": {
      "kind": "local",
      "path": "<adhoc>"
    },
    "definition": {
      "name": "google-ads-cli",
      "description": "google-ads-mcp",
      "command": {
        "kind": "stdio",
        "command": "node",
        "args": [
          "dist/index.js"
        ]
      }
    }
  },
  "artifact": {
    "path": "",
    "kind": "template"
  },
  "invocation": {
    "serverRef": "{\"name\":\"google-ads-cli\",\"command\":{\"kind\":\"stdio\",\"command\":\"node\",\"args\":[\"dist/index.js\"]},\"source\":{\"kind\":\"local\",\"path\":\"<adhoc>\"}}",
    "configPath": "<adhoc>",
    "runtime": "bun",
    "bundler": "rolldown",
    "bundle": "dist/google-ads-cli.js",
    "timeoutMs": 30000,
    "minify": false
  }
} as const;
const artifactKind = determineArtifactKind();
const program = new Command();
program.name(embeddedName);
program.description(embeddedDescription);
program.option('-t, --timeout <ms>', 'Call timeout in milliseconds', (value) => parseInt(value, 10), 30000);
program.option('-o, --output <format>', 'Output format: text|markdown|json|raw', 'text');
const commandSignatures: Record<string, string> = {
  "list-campaigns": "function list_campaigns(limit?: number, includeRemoved?: boolean, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_14_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\" | \"THIS_QUARTER\" | \"LAST_QUARTER\" | \"THIS_YEAR\" | \"LAST_YEAR\" | \"ALL_TIME\" | \"CUSTOM\", customDateRange?: unknown);",
  "get-campaign": "function get_campaign(campaignId: string);",
  "create-campaign": "function create_campaign(name: string, budget: number, advertisingChannelType: \"SEARCH\" | \"DISPLAY\" | \"SHOPPING\" | \"VIDEO\" | \"MULTI_CHANNEL\", status?: \"ENABLED\" | \"PAUSED\");",
  "update-campaign": "function update_campaign(campaignId: string, name?: string, status?: \"ENABLED\" | \"PAUSED\" | \"REMOVED\", budget?: number);",
  "list-ad-groups": "function list_ad_groups(campaignId?: string, limit?: number, includeRemoved?: boolean);",
  "create-ad-group": "function create_ad_group(campaignId: string, name: string, status?: \"ENABLED\" | \"PAUSED\", cpcBidMicros?: number, cpmBidMicros?: number);",
  "update-ad-group": "function update_ad_group(adGroupId: string, name?: string, status?: \"ENABLED\" | \"PAUSED\" | \"REMOVED\", cpcBidMicros?: number, cpmBidMicros?: number);",
  "get-ad-group": "function get_ad_group(adGroupId: string);",
  "list-ads": "function list_ads(adGroupId?: string, campaignId?: string, limit?: number, includeRemoved?: boolean);",
  "create-responsive-search-ad": "function create_responsive_search_ad(adGroupId: string, headlines: string[], descriptions: string[], path1?: string, finalUrls: string[]);",
  "update-ad": "function update_ad(adId: string, adGroupId: string, status?: \"ENABLED\" | \"PAUSED\" | \"REMOVED\");",
  "get-ad-performance": "function get_ad_performance(adId: string, adGroupId: string, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\" | \"ALL_TIME\");",
  "list-keywords": "function list_keywords(campaignId?: string, adGroupId?: string, limit?: number, includeNegative?: boolean);",
  "add-keywords": "function add_keywords(adGroupId: string, keywords: string[]);",
  "add-negative-keywords": "function add_negative_keywords(campaignId?: string, adGroupId?: string, keywords: string[]);",
  "update-keyword": "function update_keyword(keywordId: string, adGroupId: string, status?: \"ENABLED\" | \"PAUSED\" | \"REMOVED\", cpcBidMicros?: number);",
  "get-keyword-performance": "function get_keyword_performance(keywordId: string, adGroupId: string, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\");",
  "get-account-performance": "function get_account_performance(dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_14_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\" | \"THIS_QUARTER\" | \"LAST_QUARTER\", segmentByDate?: boolean);",
  "get-campaign-performance": "function get_campaign_performance(campaignId: string, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_14_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\", segmentByDate?: boolean);",
  "get-ad-group-performance": "function get_ad_group_performance(adGroupId?: string, campaignId?: string, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\", limit?: number);",
  "get-search-terms-report": "function get_search_terms_report(campaignId?: string, adGroupId?: string, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\" | \"CUSTOM\", customDateRange?: unknown, limit?: number);",
  "list-accessible-customers": "function list_accessible_customers();",
  "get-account-hierarchy": "function get_account_hierarchy(customerId?: string, loginCustomerId?: string);",
  "get-account-info": "function get_account_info(customerId: string);",
  "list-manager-accounts": "function list_manager_accounts(customerId?: string);",
  "get-top-bottom-keywords": "function get_top_bottom_keywords(metric: \"COST\" | \"CLICKS\" | \"CONVERSIONS\" | \"CTR\" | \"CONVERSION_RATE\" | \"CPC\" | \"QUALITY_SCORE\", dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\", topCount?: number, bottomCount?: number, campaignId?: string);",
  "get-keyword-opportunities": "function get_keyword_opportunities(dateRange?: \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"LAST_90_DAYS\", minImpressions?: number, maxCostPerConversion?: number, minConversionRate?: number);",
  "get-campaign-comparison": "function get_campaign_comparison(dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\", metric?: \"COST\" | \"CLICKS\" | \"CONVERSIONS\" | \"ROAS\" | \"CPA\", includeRemoved?: boolean);",
  "list-conversion-actions": "function list_conversion_actions(includeRemoved?: boolean);",
  "create-conversion-action": "function create_conversion_action(name: string, category: \"PURCHASE\" | \"SIGNUP\" | \"LEAD\" | \"PAGE_VIEW\" | \"DOWNLOAD\" | \"OTHER\", type?: \"WEBPAGE\" | \"UPLOAD_CLICKS\" | \"AD_CALL\" | \"APP_DOWNLOAD\", status?: \"ENABLED\" | \"REMOVED\" | \"HIDDEN\", valueSettings?: unknown);",
  "update-conversion-action": "function update_conversion_action(conversionActionId: string, name?: string, status?: \"ENABLED\" | \"REMOVED\" | \"HIDDEN\", valueSettings?: unknown, countingType?: \"ONE_PER_CLICK\" | \"MANY_PER_CLICK\");",
  "get-conversion-stats": "function get_conversion_stats(conversionActionId?: string, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\" | \"ALL_TIME\", segmentByConversionAction?: boolean);",
  "get-product-performance": "function get_product_performance(campaignId?: string, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\" | \"ALL_TIME\", limit?: number, orderBy?: \"COST\" | \"CLICKS\" | \"CONVERSIONS\" | \"REVENUE\" | \"ROAS\", orderDirection?: \"ASC\" | \"DESC\");",
  "get-product-partition-performance": "function get_product_partition_performance(adGroupId: string, dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\" | \"ALL_TIME\");",
  "get-top-bottom-products": "function get_top_bottom_products(metric: \"COST\" | \"CLICKS\" | \"CONVERSIONS\" | \"REVENUE\" | \"ROAS\" | \"CTR\" | \"CONVERSION_RATE\", dateRange?: \"TODAY\" | \"YESTERDAY\" | \"LAST_7_DAYS\" | \"LAST_30_DAYS\" | \"THIS_MONTH\" | \"LAST_MONTH\", topCount?: number, bottomCount?: number, campaignId?: string);"
};
program.configureHelp({
	commandTerm(cmd) {
		const term = cmd.name();
		return commandSignatures[term] ?? cmd.name();
	},
});
program.showSuggestionAfterError(true);

program
	.command("list-campaigns")
	.summary("list-campaigns [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER|THIS_YEAR|LAST_YEAR|ALL_TIME|CUSTOM>] [--custom-date-range <custom-date-range>] [--raw <json>]")
	.description("List all Google Ads campaigns with their metrics for a specific date range")
	.usage("[--limit <limit:number>] [--include-removed <include-removed:true|false>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER|THIS_YEAR|LAST_YEAR|ALL_TIME|CUSTOM>] [--custom-date-range <custom-date-range>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--limit <limit:number>", "Maximum number of campaigns to return (example: 1)", (value) => parseFloat(value))
	.option("--include-removed <include-removed:true|false>", "Include removed campaigns (example: true)", (value) => value !== 'false')
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER|THIS_YEAR|LAST_YEAR|ALL_TIME|CUSTOM>", "Date range for metrics (default: ALL_TIME) (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_14_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH, THIS_QUARTER, LAST_QUARTER, THIS_YEAR, LAST_YEAR, ALL_TIME, CUSTOM; example: TODAY)")
	.option("--custom-date-range <custom-date-range>", "Custom date range (only used when dateRange is CUSTOM)")
	
	.alias("list_campaigns")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.limit !== undefined) args.limit = cmdOpts.limit;
		if (cmdOpts.includeRemoved !== undefined) args.includeRemoved = cmdOpts.includeRemoved;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.customDateRange !== undefined) args.customDateRange = cmdOpts.customDateRange;
			const call = (proxy.listCampaigns as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.list_campaigns(limit: 1, includeRemoved: true, ...)");

program
	.command("get-campaign")
	.summary("get-campaign --campaign-id <campaign-id> [--raw <json>]")
	.description("Get detailed information about a specific campaign")
	.usage("--campaign-id <campaign-id> [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--campaign-id <campaign-id>", "Campaign ID (example: example-id)")
	
	.alias("get_campaign")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
			const call = (proxy.getCampaign as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_campaign(campaignId: \"example-id\")");

program
	.command("create-campaign")
	.summary("create-campaign --name <name> --budget <budget:number> --advertising-channel-type <advertising-channel-type:SEARCH|DISPLAY|SHOPPING|VIDEO|MULTI_CHANNEL> [--status <status:ENABLED|PAUSED>] [--raw <json>]")
	.description("Create a new Google Ads campaign")
	.usage("--name <name> --budget <budget:number> --advertising-channel-type <advertising-channel-type:SEARCH|DISPLAY|SHOPPING|VIDEO|MULTI_CHANNEL> [--status <status:ENABLED|PAUSED>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--name <name>", "Campaign name")
	.requiredOption("--budget <budget:number>", "Daily budget in account currency (example: 1)", (value) => parseFloat(value))
	.requiredOption("--advertising-channel-type <advertising-channel-type:SEARCH|DISPLAY|SHOPPING|VIDEO|MULTI_CHANNEL>", "Campaign type (choices: SEARCH, DISPLAY, SHOPPING, VIDEO, MULTI_CHANNEL; example: SEARCH)")
	.option("--status <status:ENABLED|PAUSED>", "Campaign status (choices: ENABLED, PAUSED; example: ENABLED)")
	
	.alias("create_campaign")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.name !== undefined) args.name = cmdOpts.name;
		if (cmdOpts.budget !== undefined) args.budget = cmdOpts.budget;
		if (cmdOpts.advertisingChannelType !== undefined) args.advertisingChannelType = cmdOpts.advertisingChannelType;
		if (cmdOpts.status !== undefined) args.status = cmdOpts.status;
			const call = (proxy.createCampaign as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.create_campaign(name: \"value\", budget: 1, adv, ...)");

program
	.command("update-campaign")
	.summary("update-campaign --campaign-id <campaign-id> [--name <name>] [--status <status:ENABLED|PAUSED|REMOVED>] [--budget <budget:number>] [--raw <json>]")
	.description("Update an existing campaign")
	.usage("--campaign-id <campaign-id> [--name <name>] [--status <status:ENABLED|PAUSED|REMOVED>] [--budget <budget:number>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--campaign-id <campaign-id>", "Campaign ID (example: example-id)")
	.option("--name <name>", "New campaign name")
	.option("--status <status:ENABLED|PAUSED|REMOVED>", "New campaign status (choices: ENABLED, PAUSED, REMOVED; example: ENABLED)")
	.option("--budget <budget:number>", "New daily budget (example: 1)", (value) => parseFloat(value))
	
	.alias("update_campaign")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.name !== undefined) args.name = cmdOpts.name;
		if (cmdOpts.status !== undefined) args.status = cmdOpts.status;
		if (cmdOpts.budget !== undefined) args.budget = cmdOpts.budget;
			const call = (proxy.updateCampaign as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.update_campaign(campaignId: \"example-id\", sta, ...)");

program
	.command("list-ad-groups")
	.summary("list-ad-groups [--campaign-id <campaign-id>] [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--raw <json>]")
	.description("List ad groups with their performance metrics")
	.usage("[--campaign-id <campaign-id>] [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--campaign-id <campaign-id>", "Filter by campaign ID (optional) (example: example-id)")
	.option("--limit <limit:number>", "Maximum number of ad groups to return (example: 1)", (value) => parseFloat(value))
	.option("--include-removed <include-removed:true|false>", "Include removed ad groups (example: true)", (value) => value !== 'false')
	
	.alias("list_ad_groups")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.limit !== undefined) args.limit = cmdOpts.limit;
		if (cmdOpts.includeRemoved !== undefined) args.includeRemoved = cmdOpts.includeRemoved;
			const call = (proxy.listAdGroups as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.list_ad_groups(campaignId: \"example-id\", limi, ...)");

program
	.command("create-ad-group")
	.summary("create-ad-group --campaign-id <campaign-id> --name <name> [--status <status:ENABLED|PAUSED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--cpm-bid-micros <cpm-bid-micros:number>] [--raw <json>]")
	.description("Create a new ad group in a campaign")
	.usage("--campaign-id <campaign-id> --name <name> [--status <status:ENABLED|PAUSED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--cpm-bid-micros <cpm-bid-micros:number>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--campaign-id <campaign-id>", "Campaign ID where the ad group will be created (example: example-id)")
	.requiredOption("--name <name>", "Name of the ad group")
	.option("--status <status:ENABLED|PAUSED>", "Initial status of the ad group (choices: ENABLED, PAUSED; example: ENABLED)")
	.option("--cpc-bid-micros <cpc-bid-micros:number>", "Cost per click bid in micros (1 million = 1 currency unit) (example: 1)", (value) => parseFloat(value))
	.option("--cpm-bid-micros <cpm-bid-micros:number>", "Cost per thousand impressions bid in micros (example: 1)", (value) => parseFloat(value))
	.option("--target-cpa-micros <target-cpa-micros:number>", "Target cost per acquisition in micros (example: 1)", (value) => parseFloat(value))
	.option("--target-roas <target-roas:number>", "Target return on ad spend (example: 1)", (value) => parseFloat(value))
	
	.alias("create_ad_group")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.name !== undefined) args.name = cmdOpts.name;
		if (cmdOpts.status !== undefined) args.status = cmdOpts.status;
		if (cmdOpts.cpcBidMicros !== undefined) args.cpcBidMicros = cmdOpts.cpcBidMicros;
		if (cmdOpts.cpmBidMicros !== undefined) args.cpmBidMicros = cmdOpts.cpmBidMicros;
		if (cmdOpts.targetCpaMicros !== undefined) args.targetCpaMicros = cmdOpts.targetCpaMicros;
		if (cmdOpts.targetRoas !== undefined) args.targetRoas = cmdOpts.targetRoas;
			const call = (proxy.createAdGroup as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.create_ad_group(campaignId: \"example-id\", nam, ...)")
	.addHelpText('afterAll', () => '\n' + "// optional (2): targetCpaMicros, targetRoas" + '\n');

program
	.command("update-ad-group")
	.summary("update-ad-group --ad-group-id <ad-group-id> [--name <name>] [--status <status:ENABLED|PAUSED|REMOVED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--cpm-bid-micros <cpm-bid-micros:number>] [--raw <json>]")
	.description("Update an existing ad group")
	.usage("--ad-group-id <ad-group-id> [--name <name>] [--status <status:ENABLED|PAUSED|REMOVED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--cpm-bid-micros <cpm-bid-micros:number>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID to update (example: example-id)")
	.option("--name <name>", "New name for the ad group")
	.option("--status <status:ENABLED|PAUSED|REMOVED>", "New status for the ad group (choices: ENABLED, PAUSED, REMOVED; example: ENABLED)")
	.option("--cpc-bid-micros <cpc-bid-micros:number>", "New CPC bid in micros (example: 1)", (value) => parseFloat(value))
	.option("--cpm-bid-micros <cpm-bid-micros:number>", "New CPM bid in micros (example: 1)", (value) => parseFloat(value))
	.option("--target-cpa-micros <target-cpa-micros:number>", "New target CPA in micros (example: 1)", (value) => parseFloat(value))
	.option("--target-roas <target-roas:number>", "New target ROAS (example: 1)", (value) => parseFloat(value))
	
	.alias("update_ad_group")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.name !== undefined) args.name = cmdOpts.name;
		if (cmdOpts.status !== undefined) args.status = cmdOpts.status;
		if (cmdOpts.cpcBidMicros !== undefined) args.cpcBidMicros = cmdOpts.cpcBidMicros;
		if (cmdOpts.cpmBidMicros !== undefined) args.cpmBidMicros = cmdOpts.cpmBidMicros;
		if (cmdOpts.targetCpaMicros !== undefined) args.targetCpaMicros = cmdOpts.targetCpaMicros;
		if (cmdOpts.targetRoas !== undefined) args.targetRoas = cmdOpts.targetRoas;
			const call = (proxy.updateAdGroup as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.update_ad_group(adGroupId: \"example-id\", stat, ...)")
	.addHelpText('afterAll', () => '\n' + "// optional (2): targetCpaMicros, targetRoas" + '\n');

program
	.command("get-ad-group")
	.summary("get-ad-group --ad-group-id <ad-group-id> [--raw <json>]")
	.description("Get detailed information about a specific ad group")
	.usage("--ad-group-id <ad-group-id> [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID (example: example-id)")
	
	.alias("get_ad_group")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
			const call = (proxy.getAdGroup as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_ad_group(adGroupId: \"example-id\")");

program
	.command("list-ads")
	.summary("list-ads [--ad-group-id <ad-group-id>] [--campaign-id <campaign-id>] [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--raw <json>]")
	.description("List ads with their performance metrics")
	.usage("[--ad-group-id <ad-group-id>] [--campaign-id <campaign-id>] [--limit <limit:number>] [--include-removed <include-removed:true|false>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--ad-group-id <ad-group-id>", "Filter by ad group ID (optional) (example: example-id)")
	.option("--campaign-id <campaign-id>", "Filter by campaign ID (optional) (example: example-id)")
	.option("--limit <limit:number>", "Maximum number of ads to return (example: 1)", (value) => parseFloat(value))
	.option("--include-removed <include-removed:true|false>", "Include removed ads (example: true)", (value) => value !== 'false')
	
	.alias("list_ads")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.limit !== undefined) args.limit = cmdOpts.limit;
		if (cmdOpts.includeRemoved !== undefined) args.includeRemoved = cmdOpts.includeRemoved;
			const call = (proxy.listAds as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.list_ads(adGroupId: \"example-id\", campaignId:, ...)");

program
	.command("create-responsive-search-ad")
	.summary("create-responsive-search-ad --ad-group-id <ad-group-id> --headlines <headlines:value1,value2> --descriptions <descriptions:value1,value2> [--path1 <path1>] --final-urls <final-urls:value1,value2> [--raw <json>]")
	.description("Create a new responsive search ad")
	.usage("--ad-group-id <ad-group-id> --headlines <headlines:value1,value2> --descriptions <descriptions:value1,value2> [--path1 <path1>] --final-urls <final-urls:value1,value2> [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID where the ad will be created (example: example-id)")
	.requiredOption("--headlines <headlines:value1,value2>", "Headlines for the ad (3-15 headlines) (example: value1,value2)", (value) => value.split(',').map((v) => v.trim()))
	.requiredOption("--descriptions <descriptions:value1,value2>", "Descriptions for the ad (2-4 descriptions) (example: value1,value2)", (value) => value.split(',').map((v) => v.trim()))
	.option("--path1 <path1>", "First path component (max 15 characters) (example: /path/to/file.md)")
	.option("--path2 <path2>", "Second path component (max 15 characters) (example: /path/to/file.md)")
	.requiredOption("--final-urls <final-urls:value1,value2>", "Landing page URLs (example: value1,value2)", (value) => value.split(',').map((v) => v.trim()))
	.option("--final-mobile-urls <final-mobile-urls:value1,value2>", "Mobile landing page URLs (optional) (example: value1,value2)", (value) => value.split(',').map((v) => v.trim()))
	.option("--tracking-url-template <tracking-url-template>", "Tracking URL template (optional)")
	
	.alias("create_responsive_search_ad")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.headlines !== undefined) args.headlines = cmdOpts.headlines;
		if (cmdOpts.descriptions !== undefined) args.descriptions = cmdOpts.descriptions;
		if (cmdOpts.path1 !== undefined) args.path1 = cmdOpts.path1;
		if (cmdOpts.path2 !== undefined) args.path2 = cmdOpts.path2;
		if (cmdOpts.finalUrls !== undefined) args.finalUrls = cmdOpts.finalUrls;
		if (cmdOpts.finalMobileUrls !== undefined) args.finalMobileUrls = cmdOpts.finalMobileUrls;
		if (cmdOpts.trackingUrlTemplate !== undefined) args.trackingUrlTemplate = cmdOpts.trackingUrlTemplate;
			const call = (proxy.createResponsiveSearchAd as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.create_responsive_search_ad(adGroupId: \"examp, ...)")
	.addHelpText('afterAll', () => '\n' + "// optional (3): path2, finalMobileUrls, trackingUrlTemplate" + '\n');

program
	.command("update-ad")
	.summary("update-ad --ad-id <ad-id> --ad-group-id <ad-group-id> [--status <status:ENABLED|PAUSED|REMOVED>] [--raw <json>]")
	.description("Update an existing ad status")
	.usage("--ad-id <ad-id> --ad-group-id <ad-group-id> [--status <status:ENABLED|PAUSED|REMOVED>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--ad-id <ad-id>", "Ad ID to update (example: example-id)")
	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID containing the ad (example: example-id)")
	.option("--status <status:ENABLED|PAUSED|REMOVED>", "New status for the ad (choices: ENABLED, PAUSED, REMOVED; example: ENABLED)")
	
	.alias("update_ad")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adId !== undefined) args.adId = cmdOpts.adId;
		if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.status !== undefined) args.status = cmdOpts.status;
			const call = (proxy.updateAd as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.update_ad(adId: \"example-id\", adGroupId: \"exa, ...)");

program
	.command("get-ad-performance")
	.summary("get-ad-performance --ad-id <ad-id> --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--raw <json>]")
	.description("Get detailed performance metrics for a specific ad")
	.usage("--ad-id <ad-id> --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--ad-id <ad-id>", "Ad ID (example: example-id)")
	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID containing the ad (example: example-id)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>", "Date range for metrics (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH, ALL_TIME; example: TODAY)")
	
	.alias("get_ad_performance")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adId !== undefined) args.adId = cmdOpts.adId;
		if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
			const call = (proxy.getAdPerformance as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_ad_performance(adId: \"example-id\", adGrou, ...)");

program
	.command("list-keywords")
	.summary("list-keywords [--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] [--limit <limit:number>] [--include-negative <include-negative:true|false>] [--raw <json>]")
	.description("List keywords with their performance metrics")
	.usage("[--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] [--limit <limit:number>] [--include-negative <include-negative:true|false>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--campaign-id <campaign-id>", "Filter by campaign ID (example: example-id)")
	.option("--ad-group-id <ad-group-id>", "Filter by ad group ID (example: example-id)")
	.option("--limit <limit:number>", "Maximum number of keywords to return (example: 1)", (value) => parseFloat(value))
	.option("--include-negative <include-negative:true|false>", "Include negative keywords (example: true)", (value) => value !== 'false')
	
	.alias("list_keywords")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.limit !== undefined) args.limit = cmdOpts.limit;
		if (cmdOpts.includeNegative !== undefined) args.includeNegative = cmdOpts.includeNegative;
			const call = (proxy.listKeywords as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.list_keywords(campaignId: \"example-id\", adGro, ...)");

program
	.command("add-keywords")
	.summary("add-keywords --ad-group-id <ad-group-id> --keywords <keywords:value1,value2> [--raw <json>]")
	.description("Add new keywords to an ad group")
	.usage("--ad-group-id <ad-group-id> --keywords <keywords:value1,value2> [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID (example: example-id)")
	.requiredOption("--keywords <keywords:value1,value2>", "Set keywords. (example: value1,value2)", (value) => value.split(',').map((v) => v.trim()))
	
	.alias("add_keywords")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.keywords !== undefined) args.keywords = cmdOpts.keywords;
			const call = (proxy.addKeywords as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.add_keywords(adGroupId: \"example-id\", keyword, ...)");

program
	.command("add-negative-keywords")
	.summary("add-negative-keywords [--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] --keywords <keywords:value1,value2> [--raw <json>]")
	.description("Add negative keywords to a campaign or ad group")
	.usage("[--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] --keywords <keywords:value1,value2> [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--campaign-id <campaign-id>", "Campaign ID (for campaign-level negatives) (example: example-id)")
	.option("--ad-group-id <ad-group-id>", "Ad group ID (for ad group-level negatives) (example: example-id)")
	.requiredOption("--keywords <keywords:value1,value2>", "Set keywords. (example: value1,value2)", (value) => value.split(',').map((v) => v.trim()))
	
	.alias("add_negative_keywords")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.keywords !== undefined) args.keywords = cmdOpts.keywords;
			const call = (proxy.addNegativeKeywords as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.add_negative_keywords(campaignId: \"example-id, ...)");

program
	.command("update-keyword")
	.summary("update-keyword --keyword-id <keyword-id> --ad-group-id <ad-group-id> [--status <status:ENABLED|PAUSED|REMOVED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--raw <json>]")
	.description("Update keyword status or bid")
	.usage("--keyword-id <keyword-id> --ad-group-id <ad-group-id> [--status <status:ENABLED|PAUSED|REMOVED>] [--cpc-bid-micros <cpc-bid-micros:number>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--keyword-id <keyword-id>", "Keyword ID (example: example-id)")
	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID (example: example-id)")
	.option("--status <status:ENABLED|PAUSED|REMOVED>", "New keyword status (choices: ENABLED, PAUSED, REMOVED; example: ENABLED)")
	.option("--cpc-bid-micros <cpc-bid-micros:number>", "New CPC bid in micros (example: 1)", (value) => parseFloat(value))
	
	.alias("update_keyword")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.keywordId !== undefined) args.keywordId = cmdOpts.keywordId;
		if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.status !== undefined) args.status = cmdOpts.status;
		if (cmdOpts.cpcBidMicros !== undefined) args.cpcBidMicros = cmdOpts.cpcBidMicros;
			const call = (proxy.updateKeyword as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.update_keyword(keywordId: \"example-id\", adGro, ...)");

program
	.command("get-keyword-performance")
	.summary("get-keyword-performance --keyword-id <keyword-id> --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--raw <json>]")
	.description("Get detailed performance metrics for a keyword")
	.usage("--keyword-id <keyword-id> --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--keyword-id <keyword-id>", "Keyword ID (example: example-id)")
	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID (example: example-id)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>", "Date range for metrics (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH; example: TODAY)")
	
	.alias("get_keyword_performance")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.keywordId !== undefined) args.keywordId = cmdOpts.keywordId;
		if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
			const call = (proxy.getKeywordPerformance as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_keyword_performance(keywordId: \"example-i, ...)");

program
	.command("get-account-performance")
	.summary("get-account-performance [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER>] [--segment-by-date <segment-by-date:true|false>] [--raw <json>]")
	.description("Get overall account performance metrics")
	.usage("[--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER>] [--segment-by-date <segment-by-date:true|false>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|THIS_QUARTER|LAST_QUARTER>", "Date range for metrics (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_14_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH, THIS_QUARTER, LAST_QUARTER; example: TODAY)")
	.option("--segment-by-date <segment-by-date:true|false>", "Segment results by date (example: true)", (value) => value !== 'false')
	
	.alias("get_account_performance")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.segmentByDate !== undefined) args.segmentByDate = cmdOpts.segmentByDate;
			const call = (proxy.getAccountPerformance as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_account_performance(dateRange: \"TODAY\", s, ...)");

program
	.command("get-campaign-performance")
	.summary("get-campaign-performance --campaign-id <campaign-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--segment-by-date <segment-by-date:true|false>] [--raw <json>]")
	.description("Get detailed campaign performance metrics")
	.usage("--campaign-id <campaign-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--segment-by-date <segment-by-date:true|false>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--campaign-id <campaign-id>", "Campaign ID (example: example-id)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_14_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>", "Date range for metrics (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_14_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH; example: TODAY)")
	.option("--segment-by-date <segment-by-date:true|false>", "Segment results by date (example: true)", (value) => value !== 'false')
	
	.alias("get_campaign_performance")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.segmentByDate !== undefined) args.segmentByDate = cmdOpts.segmentByDate;
			const call = (proxy.getCampaignPerformance as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_campaign_performance(campaignId: \"example, ...)");

program
	.command("get-ad-group-performance")
	.summary("get-ad-group-performance [--ad-group-id <ad-group-id>] [--campaign-id <campaign-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--limit <limit:number>] [--raw <json>]")
	.description("Get ad group performance metrics")
	.usage("[--ad-group-id <ad-group-id>] [--campaign-id <campaign-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--limit <limit:number>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--ad-group-id <ad-group-id>", "Ad group ID (optional) (example: example-id)")
	.option("--campaign-id <campaign-id>", "Filter by campaign ID (optional) (example: example-id)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>", "Date range for metrics (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH; example: TODAY)")
	.option("--limit <limit:number>", "Maximum number of ad groups to return (example: 1)", (value) => parseFloat(value))
	
	.alias("get_ad_group_performance")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.limit !== undefined) args.limit = cmdOpts.limit;
			const call = (proxy.getAdGroupPerformance as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_ad_group_performance(adGroupId: \"example-, ...)");

program
	.command("get-search-terms-report")
	.summary("get-search-terms-report [--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|CUSTOM>] [--custom-date-range <custom-date-range>] [--limit <limit:number>] [--raw <json>]")
	.description("Get search terms report to find new keyword opportunities")
	.usage("[--campaign-id <campaign-id>] [--ad-group-id <ad-group-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|CUSTOM>] [--custom-date-range <custom-date-range>] [--limit <limit:number>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--campaign-id <campaign-id>", "Filter by campaign ID (optional) (example: example-id)")
	.option("--ad-group-id <ad-group-id>", "Filter by ad group ID (optional) (example: example-id)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|CUSTOM>", "Date range for report (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH, CUSTOM; example: TODAY)")
	.option("--custom-date-range <custom-date-range>", "Custom date range (required when dateRange is CUSTOM)")
	.option("--limit <limit:number>", "Maximum number of search terms to return (example: 1)", (value) => parseFloat(value))
	.option("--min-impressions <min-impressions:number>", "Minimum impressions threshold (example: 1)", (value) => parseFloat(value))
	
	.alias("get_search_terms_report")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.customDateRange !== undefined) args.customDateRange = cmdOpts.customDateRange;
		if (cmdOpts.limit !== undefined) args.limit = cmdOpts.limit;
		if (cmdOpts.minImpressions !== undefined) args.minImpressions = cmdOpts.minImpressions;
			const call = (proxy.getSearchTermsReport as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_search_terms_report(campaignId: \"example-, ...)")
	.addHelpText('afterAll', () => '\n' + "// optional (1): minImpressions" + '\n');

program
	.command("list-accessible-customers")
	.summary("list-accessible-customers [--raw <json>]")
	.description("List all Google Ads accounts accessible by the authenticated user")
	.usage("[--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	
	.alias("list_accessible_customers")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			
			const call = (proxy.listAccessibleCustomers as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.list_accessible_customers()");

program
	.command("get-account-hierarchy")
	.summary("get-account-hierarchy [--customer-id <customer-id>] [--login-customer-id <login-customer-id>] [--raw <json>]")
	.description("Get the account hierarchy showing manager and client relationships")
	.usage("[--customer-id <customer-id>] [--login-customer-id <login-customer-id>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--customer-id <customer-id>", "Customer ID to get hierarchy for (optional) (example: example-id)")
	.option("--login-customer-id <login-customer-id>", "Login customer ID for manager accounts (optional) (example: example-id)")
	
	.alias("get_account_hierarchy")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.customerId !== undefined) args.customerId = cmdOpts.customerId;
		if (cmdOpts.loginCustomerId !== undefined) args.loginCustomerId = cmdOpts.loginCustomerId;
			const call = (proxy.getAccountHierarchy as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_account_hierarchy(customerId: \"example-id, ...)");

program
	.command("get-account-info")
	.summary("get-account-info --customer-id <customer-id> [--raw <json>]")
	.description("Get detailed information about a specific Google Ads account")
	.usage("--customer-id <customer-id> [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--customer-id <customer-id>", "Customer ID to get info for (example: example-id)")
	
	.alias("get_account_info")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.customerId !== undefined) args.customerId = cmdOpts.customerId;
			const call = (proxy.getAccountInfo as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_account_info(customerId: \"example-id\")");

program
	.command("list-manager-accounts")
	.summary("list-manager-accounts [--customer-id <customer-id>] [--raw <json>]")
	.description("List manager account relationships")
	.usage("[--customer-id <customer-id>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--customer-id <customer-id>", "Customer ID to check manager relationships (optional) (example: example-id)")
	
	.alias("list_manager_accounts")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.customerId !== undefined) args.customerId = cmdOpts.customerId;
			const call = (proxy.listManagerAccounts as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.list_manager_accounts(customerId: \"example-id\")");

program
	.command("get-top-bottom-keywords")
	.summary("get-top-bottom-keywords --metric <metric:COST|CLICKS|CONVERSIONS|CTR|CONVERSION_RATE|CPC|QUALITY_SCORE> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--top-count <top-count:number>] [--bottom-count <bottom-count:number>] [--campaign-id <campaign-id>] [--raw <json>]")
	.description("Get top and bottom performing keywords by various metrics")
	.usage("--metric <metric:COST|CLICKS|CONVERSIONS|CTR|CONVERSION_RATE|CPC|QUALITY_SCORE> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--top-count <top-count:number>] [--bottom-count <bottom-count:number>] [--campaign-id <campaign-id>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--metric <metric:COST|CLICKS|CONVERSIONS|CTR|CONVERSION_RATE|CPC|QUALITY_SCORE>", "Metric to rank keywords by (choices: COST, CLICKS, CONVERSIONS, CTR, CONVERSION_RATE, CPC, QUALITY_SCORE; example: COST)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>", "Date range for analysis (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH; example: TODAY)")
	.option("--top-count <top-count:number>", "Number of top performers to return (example: 1)", (value) => parseFloat(value))
	.option("--bottom-count <bottom-count:number>", "Number of bottom performers to return (example: 1)", (value) => parseFloat(value))
	.option("--campaign-id <campaign-id>", "Filter by specific campaign ID (optional) (example: example-id)")
	.option("--ad-group-id <ad-group-id>", "Filter by specific ad group ID (optional) (example: example-id)")
	.option("--include-negative <include-negative:true|false>", "Include negative keywords in analysis (example: true)", (value) => value !== 'false')
	
	.alias("get_top_bottom_keywords")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.metric !== undefined) args.metric = cmdOpts.metric;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.topCount !== undefined) args.topCount = cmdOpts.topCount;
		if (cmdOpts.bottomCount !== undefined) args.bottomCount = cmdOpts.bottomCount;
		if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.includeNegative !== undefined) args.includeNegative = cmdOpts.includeNegative;
			const call = (proxy.getTopBottomKeywords as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_top_bottom_keywords(metric: \"COST\", dateR, ...)")
	.addHelpText('afterAll', () => '\n' + "// optional (2): adGroupId, includeNegative" + '\n');

program
	.command("get-keyword-opportunities")
	.summary("get-keyword-opportunities [--date-range <date-range:LAST_7_DAYS|LAST_30_DAYS|LAST_90_DAYS>] [--min-impressions <min-impressions:number>] [--max-cost-per-conversion <max-cost-per-conversion:number>] [--min-conversion-rate <min-conversion-rate:number>] [--raw <json>]")
	.description("Find keyword opportunities based on search terms and underperforming keywords")
	.usage("[--date-range <date-range:LAST_7_DAYS|LAST_30_DAYS|LAST_90_DAYS>] [--min-impressions <min-impressions:number>] [--max-cost-per-conversion <max-cost-per-conversion:number>] [--min-conversion-rate <min-conversion-rate:number>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--date-range <date-range:LAST_7_DAYS|LAST_30_DAYS|LAST_90_DAYS>", "Date range for analysis (choices: LAST_7_DAYS, LAST_30_DAYS, LAST_90_DAYS; example: LAST_7_DAYS)")
	.option("--min-impressions <min-impressions:number>", "Minimum impressions threshold (example: 1)", (value) => parseFloat(value))
	.option("--max-cost-per-conversion <max-cost-per-conversion:number>", "Maximum acceptable cost per conversion (example: 1)", (value) => parseFloat(value))
	.option("--min-conversion-rate <min-conversion-rate:number>", "Minimum conversion rate percentage (example: 1)", (value) => parseFloat(value))
	
	.alias("get_keyword_opportunities")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.minImpressions !== undefined) args.minImpressions = cmdOpts.minImpressions;
		if (cmdOpts.maxCostPerConversion !== undefined) args.maxCostPerConversion = cmdOpts.maxCostPerConversion;
		if (cmdOpts.minConversionRate !== undefined) args.minConversionRate = cmdOpts.minConversionRate;
			const call = (proxy.getKeywordOpportunities as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_keyword_opportunities(dateRange: \"LAST_7_, ...)");

program
	.command("get-campaign-comparison")
	.summary("get-campaign-comparison [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--metric <metric:COST|CLICKS|CONVERSIONS|ROAS|CPA>] [--include-removed <include-removed:true|false>] [--raw <json>]")
	.description("Compare all campaigns performance by various metrics")
	.usage("[--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--metric <metric:COST|CLICKS|CONVERSIONS|ROAS|CPA>] [--include-removed <include-removed:true|false>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>", "Date range for comparison (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH; example: TODAY)")
	.option("--metric <metric:COST|CLICKS|CONVERSIONS|ROAS|CPA>", "Metric to sort campaigns by (choices: COST, CLICKS, CONVERSIONS, ROAS, CPA; example: COST)")
	.option("--include-removed <include-removed:true|false>", "Include removed campaigns in comparison (example: true)", (value) => value !== 'false')
	
	.alias("get_campaign_comparison")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.metric !== undefined) args.metric = cmdOpts.metric;
		if (cmdOpts.includeRemoved !== undefined) args.includeRemoved = cmdOpts.includeRemoved;
			const call = (proxy.getCampaignComparison as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_campaign_comparison(dateRange: \"TODAY\", m, ...)");

program
	.command("list-conversion-actions")
	.summary("list-conversion-actions [--include-removed <include-removed:true|false>] [--raw <json>]")
	.description("List all conversion actions configured in the account")
	.usage("[--include-removed <include-removed:true|false>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--include-removed <include-removed:true|false>", "Include removed conversion actions (example: true)", (value) => value !== 'false')
	
	.alias("list_conversion_actions")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.includeRemoved !== undefined) args.includeRemoved = cmdOpts.includeRemoved;
			const call = (proxy.listConversionActions as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.list_conversion_actions(includeRemoved: true)");

program
	.command("create-conversion-action")
	.summary("create-conversion-action --name <name> --category <category:PURCHASE|SIGNUP|LEAD|PAGE_VIEW|DOWNLOAD|OTHER> [--type <type:WEBPAGE|UPLOAD_CLICKS|AD_CALL|APP_DOWNLOAD>] [--status <status:ENABLED|REMOVED|HIDDEN>] [--value-settings <value-settings>] [--raw <json>]")
	.description("Create a new conversion action")
	.usage("--name <name> --category <category:PURCHASE|SIGNUP|LEAD|PAGE_VIEW|DOWNLOAD|OTHER> [--type <type:WEBPAGE|UPLOAD_CLICKS|AD_CALL|APP_DOWNLOAD>] [--status <status:ENABLED|REMOVED|HIDDEN>] [--value-settings <value-settings>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--name <name>", "Name of the conversion action")
	.requiredOption("--category <category:PURCHASE|SIGNUP|LEAD|PAGE_VIEW|DOWNLOAD|OTHER>", "Category of the conversion (choices: PURCHASE, SIGNUP, LEAD, PAGE_VIEW, DOWNLOAD, OTHER; example: PURCHASE)")
	.option("--type <type:WEBPAGE|UPLOAD_CLICKS|AD_CALL|APP_DOWNLOAD>", "Type of conversion tracking (choices: WEBPAGE, UPLOAD_CLICKS, AD_CALL, APP_DOWNLOAD; example: WEBPAGE)")
	.option("--status <status:ENABLED|REMOVED|HIDDEN>", "Status of the conversion action (choices: ENABLED, REMOVED, HIDDEN; example: ENABLED)")
	.option("--value-settings <value-settings>", "Set valueSettings.")
	.option("--counting-type <counting-type:ONE_PER_CLICK|MANY_PER_CLICK>", "How to count conversions (choices: ONE_PER_CLICK, MANY_PER_CLICK; example: ONE_PER_CLICK)")
	.option("--attribution-model <attribution-model:EXTERNAL|GOOGLE_ADS_LAST_CLICK|GOOGLE_SEARCH_ATTRIBUTION_FIRST_CLICK|GOOGLE_SEARCH_ATTRIBUTION_LINEAR|GOOGLE_SEARCH_ATTRIBUTION_TIME_DECAY|GOOGLE_SEARCH_ATTRIBUTION_POSITION_BASED|GOOGLE_SEARCH_ATTRIBUTION_DATA_DRIVEN>", "Attribution model to use (choices: EXTERNAL, GOOGLE_ADS_LAST_CLICK, GOOGLE_SEARCH_ATTRIBUTION_FIRST_CLICK, GOOGLE_SEARCH_ATTRIBUTION_LINEAR, GOOGLE_SEARCH_ATTRIBUTION_TIME_DECAY, GOOGLE_SEARCH_ATTRIBUTION_POSITION_BASED, GOOGLE_SEARCH_ATTRIBUTION_DATA_DRIVEN; example: EXTERNAL)")
	.option("--click-through-lookback-window-days <click-through-lookback-window-days:number>", "Click-through conversion window in days (1-90) (example: 1)", (value) => parseFloat(value))
	.option("--view-through-lookback-window-days <view-through-lookback-window-days:number>", "View-through conversion window in days (1-30) (example: 1)", (value) => parseFloat(value))
	
	.alias("create_conversion_action")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.name !== undefined) args.name = cmdOpts.name;
		if (cmdOpts.category !== undefined) args.category = cmdOpts.category;
		if (cmdOpts.type !== undefined) args.type = cmdOpts.type;
		if (cmdOpts.status !== undefined) args.status = cmdOpts.status;
		if (cmdOpts.valueSettings !== undefined) args.valueSettings = cmdOpts.valueSettings;
		if (cmdOpts.countingType !== undefined) args.countingType = cmdOpts.countingType;
		if (cmdOpts.attributionModel !== undefined) args.attributionModel = cmdOpts.attributionModel;
		if (cmdOpts.clickThroughLookbackWindowDays !== undefined) args.clickThroughLookbackWindowDays = cmdOpts.clickThroughLookbackWindowDays;
		if (cmdOpts.viewThroughLookbackWindowDays !== undefined) args.viewThroughLookbackWindowDays = cmdOpts.viewThroughLookbackWindowDays;
			const call = (proxy.createConversionAction as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.create_conversion_action(name: \"value\", categ, ...)")
	.addHelpText('afterAll', () => '\n' + "// optional (4): countingType, attributionModel, clickThroughLookbackWindowDays, viewThroughLookbackWindowDays" + '\n');

program
	.command("update-conversion-action")
	.summary("update-conversion-action --conversion-action-id <conversion-action-id> [--name <name>] [--status <status:ENABLED|REMOVED|HIDDEN>] [--value-settings <value-settings>] [--counting-type <counting-type:ONE_PER_CLICK|MANY_PER_CLICK>] [--raw <json>]")
	.description("Update an existing conversion action")
	.usage("--conversion-action-id <conversion-action-id> [--name <name>] [--status <status:ENABLED|REMOVED|HIDDEN>] [--value-settings <value-settings>] [--counting-type <counting-type:ONE_PER_CLICK|MANY_PER_CLICK>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--conversion-action-id <conversion-action-id>", "ID of the conversion action to update (example: example-id)")
	.option("--name <name>", "New name for the conversion action")
	.option("--status <status:ENABLED|REMOVED|HIDDEN>", "New status (choices: ENABLED, REMOVED, HIDDEN; example: ENABLED)")
	.option("--value-settings <value-settings>", "Set valueSettings.")
	.option("--counting-type <counting-type:ONE_PER_CLICK|MANY_PER_CLICK>", "How to count conversions (choices: ONE_PER_CLICK, MANY_PER_CLICK; example: ONE_PER_CLICK)")
	.option("--attribution-model <attribution-model:EXTERNAL|GOOGLE_ADS_LAST_CLICK|GOOGLE_SEARCH_ATTRIBUTION_FIRST_CLICK|GOOGLE_SEARCH_ATTRIBUTION_LINEAR|GOOGLE_SEARCH_ATTRIBUTION_TIME_DECAY|GOOGLE_SEARCH_ATTRIBUTION_POSITION_BASED|GOOGLE_SEARCH_ATTRIBUTION_DATA_DRIVEN>", "Attribution model (choices: EXTERNAL, GOOGLE_ADS_LAST_CLICK, GOOGLE_SEARCH_ATTRIBUTION_FIRST_CLICK, GOOGLE_SEARCH_ATTRIBUTION_LINEAR, GOOGLE_SEARCH_ATTRIBUTION_TIME_DECAY, GOOGLE_SEARCH_ATTRIBUTION_POSITION_BASED, GOOGLE_SEARCH_ATTRIBUTION_DATA_DRIVEN; example: EXTERNAL)")
	.option("--click-through-lookback-window-days <click-through-lookback-window-days:number>", "Click-through window in days (example: 1)", (value) => parseFloat(value))
	.option("--view-through-lookback-window-days <view-through-lookback-window-days:number>", "View-through window in days (example: 1)", (value) => parseFloat(value))
	
	.alias("update_conversion_action")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.conversionActionId !== undefined) args.conversionActionId = cmdOpts.conversionActionId;
		if (cmdOpts.name !== undefined) args.name = cmdOpts.name;
		if (cmdOpts.status !== undefined) args.status = cmdOpts.status;
		if (cmdOpts.valueSettings !== undefined) args.valueSettings = cmdOpts.valueSettings;
		if (cmdOpts.countingType !== undefined) args.countingType = cmdOpts.countingType;
		if (cmdOpts.attributionModel !== undefined) args.attributionModel = cmdOpts.attributionModel;
		if (cmdOpts.clickThroughLookbackWindowDays !== undefined) args.clickThroughLookbackWindowDays = cmdOpts.clickThroughLookbackWindowDays;
		if (cmdOpts.viewThroughLookbackWindowDays !== undefined) args.viewThroughLookbackWindowDays = cmdOpts.viewThroughLookbackWindowDays;
			const call = (proxy.updateConversionAction as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.update_conversion_action(conversionActionId:, ...)")
	.addHelpText('afterAll', () => '\n' + "// optional (3): attributionModel, clickThroughLookbackWindowDays, viewThroughLookbackWindowDays" + '\n');

program
	.command("get-conversion-stats")
	.summary("get-conversion-stats [--conversion-action-id <conversion-action-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--segment-by-conversion-action <segment-by-conversion-action:true|false>] [--raw <json>]")
	.description("Get conversion statistics and performance")
	.usage("[--conversion-action-id <conversion-action-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--segment-by-conversion-action <segment-by-conversion-action:true|false>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--conversion-action-id <conversion-action-id>", "Filter by specific conversion action ID (optional) (example: example-id)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>", "Date range for statistics (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH, ALL_TIME; example: TODAY)")
	.option("--segment-by-conversion-action <segment-by-conversion-action:true|false>", "Show breakdown by conversion action (example: true)", (value) => value !== 'false')
	
	.alias("get_conversion_stats")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.conversionActionId !== undefined) args.conversionActionId = cmdOpts.conversionActionId;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.segmentByConversionAction !== undefined) args.segmentByConversionAction = cmdOpts.segmentByConversionAction;
			const call = (proxy.getConversionStats as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_conversion_stats(conversionActionId: \"exa, ...)");

program
	.command("get-product-performance")
	.summary("get-product-performance [--campaign-id <campaign-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--limit <limit:number>] [--order-by <order-by:COST|CLICKS|CONVERSIONS|REVENUE|ROAS>] [--order-direction <order-direction:ASC|DESC>] [--raw <json>]")
	.description("Get performance data for products in shopping campaigns")
	.usage("[--campaign-id <campaign-id>] [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--limit <limit:number>] [--order-by <order-by:COST|CLICKS|CONVERSIONS|REVENUE|ROAS>] [--order-direction <order-direction:ASC|DESC>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.option("--campaign-id <campaign-id>", "Filter by specific campaign ID (optional) (example: example-id)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>", "Date range for metrics (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH, ALL_TIME; example: TODAY)")
	.option("--limit <limit:number>", "Maximum number of products to return (example: 1)", (value) => parseFloat(value))
	.option("--order-by <order-by:COST|CLICKS|CONVERSIONS|REVENUE|ROAS>", "Metric to order by (choices: COST, CLICKS, CONVERSIONS, REVENUE, ROAS; example: COST)")
	.option("--order-direction <order-direction:ASC|DESC>", "Order direction (choices: ASC, DESC; example: ASC)")
	
	.alias("get_product_performance")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.limit !== undefined) args.limit = cmdOpts.limit;
		if (cmdOpts.orderBy !== undefined) args.orderBy = cmdOpts.orderBy;
		if (cmdOpts.orderDirection !== undefined) args.orderDirection = cmdOpts.orderDirection;
			const call = (proxy.getProductPerformance as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_product_performance(campaignId: \"example-, ...)");

program
	.command("get-product-partition-performance")
	.summary("get-product-partition-performance --ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--raw <json>]")
	.description("Get performance data for product partitions in an ad group")
	.usage("--ad-group-id <ad-group-id> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--ad-group-id <ad-group-id>", "Ad group ID containing the product partitions (example: example-id)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH|ALL_TIME>", "Date range for metrics (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH, ALL_TIME; example: TODAY)")
	
	.alias("get_product_partition_performance")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.adGroupId !== undefined) args.adGroupId = cmdOpts.adGroupId;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
			const call = (proxy.getProductPartitionPerformance as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_product_partition_performance(adGroupId:, ...)");

program
	.command("get-top-bottom-products")
	.summary("get-top-bottom-products --metric <metric:COST|CLICKS|CONVERSIONS|REVENUE|ROAS|CTR|CONVERSION_RATE> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--top-count <top-count:number>] [--bottom-count <bottom-count:number>] [--campaign-id <campaign-id>] [--raw <json>]")
	.description("Get top and bottom performing products by various metrics")
	.usage("--metric <metric:COST|CLICKS|CONVERSIONS|REVENUE|ROAS|CTR|CONVERSION_RATE> [--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>] [--top-count <top-count:number>] [--bottom-count <bottom-count:number>] [--campaign-id <campaign-id>] [--raw <json>]")
	.option('--raw <json>', 'Provide raw JSON arguments to the tool, bypassing flag parsing.')

	.requiredOption("--metric <metric:COST|CLICKS|CONVERSIONS|REVENUE|ROAS|CTR|CONVERSION_RATE>", "Metric to rank products by (choices: COST, CLICKS, CONVERSIONS, REVENUE, ROAS, CTR, CONVERSION_RATE; example: COST)")
	.option("--date-range <date-range:TODAY|YESTERDAY|LAST_7_DAYS|LAST_30_DAYS|THIS_MONTH|LAST_MONTH>", "Date range for analysis (choices: TODAY, YESTERDAY, LAST_7_DAYS, LAST_30_DAYS, THIS_MONTH, LAST_MONTH; example: TODAY)")
	.option("--top-count <top-count:number>", "Number of top performers to return (example: 1)", (value) => parseFloat(value))
	.option("--bottom-count <bottom-count:number>", "Number of bottom performers to return (example: 1)", (value) => parseFloat(value))
	.option("--campaign-id <campaign-id>", "Filter by specific campaign ID (optional) (example: example-id)")
	
	.alias("get_top_bottom_products")	.action(async (cmdOpts) => {
		const globalOptions = program.opts();
		const runtime = await ensureRuntime();
		const serverName = embeddedName;
		const proxy = createServerProxy(runtime, serverName, {
			initialSchemas: embeddedSchemas,
		});
		try {
			const args = cmdOpts.raw ? JSON.parse(cmdOpts.raw) : ({} as Record<string, unknown>);
			if (cmdOpts.metric !== undefined) args.metric = cmdOpts.metric;
		if (cmdOpts.dateRange !== undefined) args.dateRange = cmdOpts.dateRange;
		if (cmdOpts.topCount !== undefined) args.topCount = cmdOpts.topCount;
		if (cmdOpts.bottomCount !== undefined) args.bottomCount = cmdOpts.bottomCount;
		if (cmdOpts.campaignId !== undefined) args.campaignId = cmdOpts.campaignId;
			const call = (proxy.getTopBottomProducts as any)(args);
			const result = await invokeWithTimeout(call, globalOptions.timeout || 30000);
			printResult(result, globalOptions.output ?? 'text');
		} finally {
			await runtime.close(serverName).catch(() => {});
		}
	})
	.addHelpText('after', () => '\nExample:\n  ' + "mcporter call google-ads-cli.get_top_bottom_products(metric: \"COST\", dateR, ...)");

program
	.command('__mcporter_inspect', { hidden: true })
	.description('Internal metadata printer for mcporter inspect-cli.')
	.action(() => {
		const payload = buildMetadataPayload();
		console.log(JSON.stringify(payload, null, 2));
	});

configureToolCommandHelps();

const FORCE_COLOR = process.env.FORCE_COLOR?.toLowerCase();
const forceDisableColor = FORCE_COLOR === '0' || FORCE_COLOR === 'false';
const forceEnableColor = FORCE_COLOR === '1' || FORCE_COLOR === 'true' || FORCE_COLOR === '2' || FORCE_COLOR === '3';
const hasNoColor = process.env.NO_COLOR !== undefined;
const stdoutStream = process.stdout as NodeJS.WriteStream | undefined;
const supportsAnsiColor = !hasNoColor && (forceEnableColor || (!forceDisableColor && Boolean(stdoutStream?.isTTY)));

const tint = {
	bold(text: string): string {
		return supportsAnsiColor ? '[1m' + text + '[0m' : text;
	},
	dim(text: string): string {
		return supportsAnsiColor ? '[90m' + text + '[0m' : text;
	},
	extraDim(text: string): string {
		return supportsAnsiColor ? '[38;5;244m' + text + '[0m' : text;
	},
};

function configureGeneratedCommandHelp(command: Command): void {
	command.configureHelp({
		commandUsage(target) {
			const usage = (target.name() + ' ' + target.usage()).trim() || target.name();
			return supportsAnsiColor ? tint.bold(usage) : usage;
		},
		optionTerm(option) {
			const term = option.flags ?? '';
			return supportsAnsiColor ? tint.bold(term) : term;
		},
		optionDescription(option) {
			const description = option.description ?? '';
			return supportsAnsiColor ? tint.extraDim(description) : description;
		},
	});
}

function configureToolCommandHelps(): void {
	program.commands.forEach((cmd) => {
		if (cmd.name() === '__mcporter_inspect') {
			return;
		}
		configureGeneratedCommandHelp(cmd);
	});
}

function renderStandaloneHelp(): string {
	const colorfulTitle = tint.bold(embeddedName) + ' ' + tint.dim('— ' + embeddedDescription);
	const plainTitle = embeddedName + ' — ' + embeddedDescription;
	const title = supportsAnsiColor ? colorfulTitle : plainTitle;
	const lines = [title, '', 'Usage: ' + embeddedName + ' <command> [options]', ''];
	if (generatorTools) {
		lines.push(formatEmbeddedTools());
	}
	lines.push('', formatGlobalFlags(), '', formatQuickStart());
	if (generatorInfo) {
		lines.push('', tint.extraDim(generatorInfo));
	}
	return lines.join('\n');
}

program.helpInformation = () => renderStandaloneHelp();

function formatEmbeddedTools(): string {
	const header = supportsAnsiColor ? tint.bold('Embedded tools') : 'Embedded tools';
	if (!generatorTools.length) {
		return header;
	}
	const lines = [header];
	generatorTools.forEach((entry) => {
		const renderedDesc = entry.description
			? supportsAnsiColor
				? tint.extraDim(entry.description)
				: entry.description
			: undefined;
		const base = renderedDesc ? entry.name + ' - ' + renderedDesc : entry.name;
		lines.push('  ' + base);
		if (entry.flags) {
			const renderedFlags = supportsAnsiColor ? tint.extraDim(entry.flags) : entry.flags;
			lines.push('    ' + renderedFlags);
		}
		lines.push('');
	});
	if (lines[lines.length - 1] === '') {
		lines.pop();
	}
	return lines.join('\n');
}

function formatGlobalFlags(): string {
	const header = supportsAnsiColor ? tint.bold('Global flags') : 'Global flags';
	const entries = [
		['-t, --timeout <ms>', 'Call timeout in milliseconds'],
		['-o, --output <format>', 'text | markdown | json | raw (default text)'],
	];
	const formatted = entries.map(([flag, summary]) => '  ' + flag.padEnd(28) + summary);
	return [header, ...formatted].join('\n');
}

function formatQuickStart(): string {
  const header = supportsAnsiColor ? tint.bold('Quick start') : 'Quick start';
  const examples = quickStartExamples();
  if (!examples.length) {
    return header;
  }
  const formatted = examples.map(([cmd, note]) => '  ' + cmd + '\n    ' + tint.dim('# ' + note));
  return [header, ...formatted].join('\n');
}

function quickStartExamples(): Array<[string, string]> {
  const examples: Array<[string, string]> = [];
  const commandMap = new Map<string, string>();
  program.commands.forEach((cmd) => {
    const name = cmd.name();
    if (name !== '__mcporter_inspect') {
      commandMap.set(name, name);
    }
  });
  const embedded = Array.isArray(generatorTools) ? generatorTools : [];
  for (const entry of embedded.slice(0, 3)) {
    const commandName = commandMap.get(entry.name) ?? entry.name;
    const flags = entry.flags ? ' ' + entry.flags.replace(/<[^>]+>/g, '<value>') : '';
    examples.push([embeddedName + ' ' + commandName + flags, 'invoke ' + commandName]);
  }
  if (!examples.length) {
    examples.push([embeddedName + ' <tool> --key value', 'invoke a tool with flags']);
  }
  return examples;
}

function printResult(result: unknown, format: string) {
	const wrapped = createCallResult(result);
	switch (format) {
		case 'json': {
			const json = wrapped.json();
			if (json) {
				console.log(JSON.stringify(json, null, 2));
				return;
			}
			break;
		}
		case 'markdown': {
			const markdown = wrapped.markdown();
			if (markdown) {
				console.log(markdown);
				return;
			}
			break;
		}
		case 'raw': {
			console.log(JSON.stringify(wrapped.raw, null, 2));
			return;
		}
	}
	const text = wrapped.text();
	if (text) {
		console.log(text);
	} else {
		console.log(JSON.stringify(wrapped.raw, null, 2));
	}
}

function normalizeEmbeddedServer(server: typeof embeddedServer) {
	const base = { ...server } as Record<string, unknown>;
	if ((server.command as any).kind === 'http') {
		const urlRaw = (server.command as any).url;
		const urlValue = typeof urlRaw === 'string' ? urlRaw : String(urlRaw);
		return {
			...base,
			command: {
				...(server.command as Record<string, unknown>),
				url: new URL(urlValue),
			},
		};
	}
	if ((server.command as any).kind === 'stdio') {
		return {
			...base,
			command: {
				...(server.command as Record<string, unknown>),
				args: [ ...((server.command as any).args ?? []) ],
			},
		};
	}
	return base;
}

function determineArtifactKind(): 'template' | 'bundle' | 'binary' {
	const scriptPath = typeof process !== 'undefined' && Array.isArray(process.argv) ? process.argv[1] ?? '' : '';
	if (scriptPath.endsWith('.ts')) {
		return 'template';
	}
	if (scriptPath.endsWith('.js')) {
		return 'bundle';
	}
	return 'binary';
}

function resolveArtifactPath(): string {
	if (typeof process !== 'undefined' && Array.isArray(process.argv) && process.argv.length > 1) {
		const script = process.argv[1];
		if (script) {
			return script;
		}
	}
	return embeddedMetadata.artifact.path;
}

function buildMetadataPayload() {
	const invocation = { ...embeddedMetadata.invocation };
	const path = resolveArtifactPath();
	if (artifactKind === 'template' && path) {
		invocation.outputPath = invocation.outputPath ?? path;
	} else if (artifactKind === 'bundle' && path) {
		invocation.bundle = invocation.bundle ?? path;
	} else if (artifactKind === 'binary' && path) {
		invocation.compile = invocation.compile ?? path;
	}
	return {
		...embeddedMetadata,
		artifact: {
			path,
			kind: artifactKind,
		},
		invocation,
	};
}

async function ensureRuntime(): Promise<Awaited<ReturnType<typeof createRuntime>>> {
	return await createRuntime({
		servers: [normalizeEmbeddedServer(embeddedServer)],
	});
}

async function invokeWithTimeout<T>(call: Promise<T>, timeout: number): Promise<T> {
	if (!Number.isFinite(timeout) || timeout <= 0) {
		return await call;
	}
	let timer: ReturnType<typeof setTimeout> | undefined;
	try {
		return await Promise.race([
			call,
			new Promise<never>((_, reject) => {
				timer = setTimeout(() => {
					reject(new Error('Call timed out after ' + timeout + 'ms.'));
				}, timeout);
			}),
		]);
	} finally {
		if (timer) {
			clearTimeout(timer);
		}
	}
}

async function runCli(): Promise<void> {
	const args = process.argv.slice(2);
	if (args.length === 0) {
		program.outputHelp();
		return;
	}
	await program.parseAsync(process.argv);
}

if (process.env.MCPORTER_DISABLE_AUTORUN !== '1') {
	runCli().catch((error) => {
		const message = error instanceof Error ? error.message : String(error);
		console.error(message);
		process.exit(1);
	});
}
